---
title: "Analyse Spectrale - `r params$pays`"
author: "Projet Atlas Spectral Sahel"
date: "`r Sys.Date()`"
output: html_document
params:
  pays: "Mali"
  categorie: "vegetation"
  data_file: "data/indicateurs_moyens_par_region.csv"
---

```{r setup, include=FALSE}
library(dplyr)
library(ggplot2)
library(readr)
library(leaflet)
library(sf)

# 📂 Charger les données
df <- read_csv(params$data_file)

# 📌 Filtrer les données pour le pays et la catégorie d'indice
df_pays <- df %>% filter(pays == params$pays)

# Charger le shapefile
shp_file <- paste0("data/shapes/regions_", params$pays, ".shp")
regions <- st_read(shp_file, quiet = TRUE)

# Vérifier la structure des données
head(df_pays)
```

